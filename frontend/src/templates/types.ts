export interface TemplateProps {
    pd: any;
    data: any;
    color: string;
    rgb: string;
    mode: string;
    hiddenSections: Set<string>;
    initials: string;
    isPdf: boolean;
    publicUrl: string;
    isPreview: boolean;
}
